<% if params[:action] == 'index' && ['blogs', 'tour_packages'].include?(params[:controller])%>
<div class="header collapse d-lg-flex p-0" style="background-color:#132952;">
  <div class="container">
    <div class="row align-items-center  h-6 pr-4">
      <ul class="nav nav-tabs border-0 flex-column flex-lg-row text-white">
          <li class="nav-item" ><%=link_to t("new_#{params[:controller]}"), "#{params[:controller]}/new", style: "color: #fff;"%></li>
      </ul>
    </div>
  </div>
</div>
<%end%>
